#include <stdio.h>
#include <stdlib.h>

int fadd (int a, int b);
int fsus (int a, int b);
int fmul (int a, int b);
int fdiv (int a, int b);

// revizar video 43

int main()
{
    int a = 0;
    int x = 10, y = 2;
    int p (int, int);
    p == fadd(10, 2);


printf("%d\n", p);
printf("%d\n", fsus(x, y));
printf("%d\n", fmul(x, y));
printf("%d\n", fdiv(x, y));
   return 0;
}


int fadd (int a, int b)
{
    return a + b;
}

int fsus (int a, int b)
{
    return a - b;

}
int fmul (int a, int b)
{
        return a * b;
}

int fdiv (int a, int b)
{
        return a / b;

}
